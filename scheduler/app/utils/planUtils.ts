// No transformation needed - use plans exactly as defined in CAREER_PLANS
export const normalizePlanId = (planId: string) => {
  return planId;
};

export const denormalizePlanId = (planId: string) => {
  return planId;
}; 